{
  "name": "deriv-bot-backend",
  "version": "1.0.0",
  "description": "Backend for Deriv Bot — proxies the OAuth2 + PKCE code/token exchange for the frontend",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  }
}
